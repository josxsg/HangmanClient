<Window x:Class="HangmanClient.JoinMatch"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HangmanClient"
        mc:Ignorable="d"
        xmlns:p="clr-namespace:HangmanClient.Properties"
        WindowState="Maximized" ResizeMode="CanResize" WindowStartupLocation="CenterScreen">
    <Grid>
        <Image Source="/Properties/Images/Match.png" Grid.ColumnSpan="2" Stretch="UniformToFill" Opacity="0.5"/>
        <Border Style="{StaticResource bdrCuadroTexto}"          
               Margin="50" Padding="30">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15">
                    <Button x:Name="btnBack" Content="◄ VOLVER" 
                            Style="{StaticResource btnMain}" Background="#A0522D"
                            Width="140" Height="45" FontSize="18" Margin="0,0,30,0" 
                            Click="btnBack_Click"/>

                    <Label Content="PARTIDAS DISPONIBLES" Style="{StaticResource lblTitulo}" FontSize="40" VerticalAlignment="Center"/>
                </StackPanel>

                <Border Grid.Row="1" Height="4" Background="#8B4513" Margin="0,0,0,20" CornerRadius="2"/>

                <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">

                    <ItemsControl x:Name="listaPartidas">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>

                                <Border BorderBrush="#8B4513" BorderThickness="0,0,0,2" Padding="10,20">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <StackPanel Grid.Column="0" VerticalAlignment="Center">

                                            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                                                <TextBlock Text="{Binding IdPartida}" Style="{StaticResource txtCuadro}" FontSize="20" FontWeight="Bold" Foreground="#8B4513" Margin="0,0,30,0"/>
                                                <TextBlock Text="{Binding FechaCreacion}" Style="{StaticResource txtCuadro}" FontSize="18" VerticalAlignment="Center"/>
                                            </StackPanel>

                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="Creada Por: " Style="{StaticResource txtCuadro}" FontSize="18" FontWeight="Bold"/>
                                                <TextBlock Text="{Binding NombreUsuario}" Style="{StaticResource txtCuadro}" FontSize="18" Margin="0,0,15,0"/>
                                                <TextBlock Text="{Binding Correo}" Style="{StaticResource txtCuadro}" FontSize="16" FontStyle="Italic" Foreground="#5C4033" VerticalAlignment="Center"/>
                                            </StackPanel>

                                        </StackPanel>

                                        <Button Grid.Column="1" Content="UNIRSE" 
                                                Style="{StaticResource btnMain}" 
                                                Width="160" Height="50" FontSize="20" VerticalAlignment="Center"
                                                Tag="{Binding IdPartida}"
                                                Click="btnJoin_Click"/>
                                    </Grid>
                                </Border>

                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                </ScrollViewer>
            </Grid>
        </Border>
    </Grid>
</Window>
